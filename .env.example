# MEDXI environment variables example
#
# This file lists variables used by both backend (`server`) and frontend (`client`).
# For local development:
# 1) Copy the SERVER section into `server/.env`
# 2) Copy the CLIENT section into `client/.env`

# =========================
# SERVER (.env in /server)
# =========================
NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:5173

MONGODB_URI=mongodb://127.0.0.1:27017/virtualhealthcompanion

JWT_SECRET=replace_with_strong_access_secret
JWT_EXPIRE=1h
JWT_REFRESH_SECRET=replace_with_strong_refresh_secret
JWT_REFRESH_EXPIRE=7d

# Gemini AI key
GEMINI_API_KEY=your_gemini_api_key

# Email mode (maildev|smtp)
EMAIL_MODE=maildev

# Google auth (used by app login/signup)
GOOGLE_AUTH_CLIENT_ID=your_google_oauth_client_id
GOOGLE_AUTH_CLIENT_SECRET=your_google_oauth_client_secret
GOOGLE_AUTH_REDIRECT_URI=http://localhost:5000/api/auth/google/callback

# Google Fit integration (can reuse same OAuth app credentials if desired)
GOOGLE_FIT_CLIENT_ID=your_google_fit_client_id
GOOGLE_FIT_CLIENT_SECRET=your_google_fit_client_secret
GOOGLE_FIT_REDIRECT_URI=http://localhost:5000/api/googlefit/callback

# =========================
# CLIENT (.env in /client)
# =========================
VITE_API_URL=http://localhost:5000/api
