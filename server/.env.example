# Backend Environment Variables
PORT=5000
NODE_ENV=development

# MongoDB
MONGODB_URI=your_mongodb_connection_string

# JWT
JWT_SECRET=your_jwt_secret_key_change_in_production
JWT_REFRESH_SECRET=your_jwt_refresh_secret_key_change_in_production
JWT_EXPIRE=1h
JWT_REFRESH_EXPIRE=7d

# Google Gemini API
GEMINI_API_KEY=your_gemini_api_key

# Google Fit API
GOOGLE_FIT_CLIENT_ID=your_google_fit_client_id
GOOGLE_FIT_CLIENT_SECRET=your_google_fit_client_secret
GOOGLE_FIT_REDIRECT_URI=http://localhost:5000/api/googlefit/callback

# CORS
CLIENT_URL=http://localhost:5173

# ==========================================
# EMAIL SERVICE CONFIGURATION
# ==========================================
# Toggle between local testing (Maildev) and real email sending (Gmail)
# Options: "maildev" or "gmail"
EMAIL_MODE=maildev

# -----------------------------------------
# MODE 1: Maildev (Local Testing - Default)
# -----------------------------------------
# Emails are captured locally and viewed in web UI
# No real emails are sent - perfect for development
# 
# Setup: docker compose -f docker-compose.dev.yml up maildev
# View emails: http://localhost:1080
#
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM_EMAIL=noreply@medxi.local

# -----------------------------------------
# MODE 2: Gmail SMTP (Real Email Sending)
# -----------------------------------------
# Send real emails to actual Gmail/Yahoo/etc. addresses
# Perfect for demos or production-like testing
#
# Setup Instructions:
# 1. Go to https://myaccount.google.com/apppasswords
# 2. Sign in to your Google account
# 3. Create new app password (name it "MEDXI")
# 4. Copy the 16-character password
# 5. Paste below (remove spaces)
# 6. Set EMAIL_MODE=gmail above
#
# GMAIL_USER=your-email@gmail.com
# GMAIL_APP_PASSWORD=your-16-char-app-password
#
# ⚠️ NEVER use your regular Gmail password!
# ⚠️ Must use App Password (requires 2FA enabled)

# ==========================================
# USAGE EXAMPLES
# ==========================================
# Local development (no real emails):
#   EMAIL_MODE=maildev npm start
#
# Demo with real emails:
#   EMAIL_MODE=gmail npm start
#
# Toggle anytime by changing EMAIL_MODE variable
