# Backend Environment Variables
PORT=5000
NODE_ENV=development

# MongoDB
MONGODB_URI=your_mongodb_connection_string

# JWT
JWT_SECRET=your_jwt_secret_key_change_in_production
JWT_REFRESH_SECRET=your_jwt_refresh_secret_key_change_in_production
JWT_EXPIRE=1h
JWT_REFRESH_EXPIRE=7d

# Google Gemini API
GEMINI_API_KEY=your_gemini_api_key

# Google Fit API
GOOGLE_FIT_CLIENT_ID=your_google_fit_client_id
GOOGLE_FIT_CLIENT_SECRET=your_google_fit_client_secret
GOOGLE_FIT_REDIRECT_URI=http://localhost:5000/api/googlefit/callback

# CORS
CLIENT_URL=http://localhost:5173

# ==========================================
# EMAIL SERVICE CONFIGURATION
# ==========================================
# The email service supports multiple providers with automatic fallback.
# Configure at least one provider for email functionality.

# SendGrid (Primary - Recommended)
# Sign up at: https://sendgrid.com
SENDGRID_API_KEY=your_sendgrid_api_key
SENDGRID_FROM_EMAIL=noreply@medxi.com

# Mailgun (Fallback)
# Sign up at: https://mailgun.com
MAILGUN_API_KEY=your_mailgun_api_key
MAILGUN_DOMAIN=your_mailgun_domain
MAILGUN_FROM_EMAIL=noreply@medxi.com

# SMTP (Development Fallback)
# For local testing, use Maildev: docker compose -f docker-compose.dev.yml up maildev
# Maildev runs on localhost:1025 (SMTP) and localhost:1080 (Web UI)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM_EMAIL=noreply@medxi.local

# ==========================================
# EMAIL CONFIGURATION NOTES
# ==========================================
# - The service tries providers in order: SendGrid → Mailgun → SMTP
# - Each provider retries failed sends up to 3 times with exponential backoff
# - All email attempts are logged to the audit log
# - In development mode, verification/reset tokens are returned in API responses for testing
# - For production, ensure at least SendGrid or Mailgun is configured
